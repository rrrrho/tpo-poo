package com.poo.trilateracion.model;
/**
 * Representa un vector bidimensional con componentes en el plano XY.
 * Esta clase extiende la clase Tupla y representa un vector con coordenadas (x, y).
 */
public final class Vector extends Tupla {
    public Vector(double x, double y) {
        super(x, y);
    }
}
